/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OnOffPipe } from './onOff.pipe';

describe('Pipe: OnOffe', () => {
  it('create an instance', () => {
    let pipe = new OnOffPipe();
    expect(pipe).toBeTruthy();
  });
});